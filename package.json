{
  "type": "module",
  "devDependencies": {
    "@commander-js/extra-typings": "^12.1.0",
    "@types/node": "^22.10.2",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "test": "tsx --test ./test/*.test.ts",
    "build": "tsc && tsc-alias",
    "execution": "node ./dist/app.js -f url_list.txt -c config.json -v"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "ts-node": "^10.9.2"
  }
}
